-- Check current RLS policies for TeamMember table
SELECT 
    schemaname,
    tablename,
    policyname,
    permissive,
    roles,
    cmd,
    qual,
    with_check
FROM pg_policies 
WHERE tablename = 'TeamMember'
ORDER BY policyname;
