/**
 * Honest copy for the Accessibility Menu.
 * Never claim the menu makes a site ADA / AODA / WCAG compliant.
 * Visitor strings live in translations.ts (EN + FR).
 */

/** Owner-facing. Use in the builder, dashboard, install step, Terms. */
export const A11Y_OWNER_DISCLAIMER =
  'This menu lets visitors adjust how the page looks (font size, contrast, motion). It does not make a website WCAG, AODA or ADA compliant. Content, forms and images still need to be built accessibly.'

/** Tight UI: dashboard cards, install notes, HTML comments. */
export const A11Y_OWNER_DISCLAIMER_SHORT =
  'Does not make a website WCAG, AODA or ADA compliant.'
